# main application module

# imports "app" the variable from "app" the package
# from directory import variable
from app import app


# to run, type the following into terminal while in the microblog directory:

#   source virtenv/bin/activate
#   export FLASK_APP=microblog.py
#   flask run

# this first activates the virtual environment, 
# then defines which program to run via the flask command,
# and finally runs it